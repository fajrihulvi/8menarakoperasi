; open_basedir lama menunjuk ke /www/wwwroot/8mp.store/ (path server hosting)
; yang tidak ada di komputer ini, sehingga PHP menolak membaca semua file.
; Dinonaktifkan untuk development lokal.
; Di server produksi, kembalikan ke path docroot yang benar.

; open_basedir=/www/wwwroot/8mp.store/:/tmp/
